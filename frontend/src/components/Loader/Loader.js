import React from 'react';
import '../Loader/Loader.css'; // Import the loader CSS file

const Loader = () => {
  return (
    <div className="loader-wrapper">
      <span className="loader"></span>
    </div>
  );
};

export default Loader;
